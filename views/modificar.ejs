<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Blog</title>
  <link rel="stylesheet" href="/styles/modificar.css">
</head>

<body>
  <div class="container">
    <h1>
      <%= heading %>
    </h1>

    <% if (locals.notes) { %>
      <form id="editForm" method="post" action="/api/notes/<%=notes.id%>">
        <input type="text" name="title" value="<%=notes.title %>">
        <textarea name="text" rows="10"><%=notes.noteText %></textarea>
        <input type="text" name="type" value="<%=notes.noteType %>">
        <button class="full-width" type="submit ">
            <%= submit %>
        </button>
      </form>
      <% } else {%>
        <form id="newPostForm" method="post" action="/api/notes">
          <input type="text" name="title" placeholder="Title" required>
          <textarea name="text" placeholder="Content" required rows="10"></textarea>
          <input type="text" name="type" placeholder="Type" required>
          <button class="full-width" type="submit">
            <%= submit %>
          </button>
        </form>
        <% } %>

  </div>
</body>

</html>
